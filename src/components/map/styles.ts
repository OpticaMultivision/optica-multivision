import type { SxProps } from '@mui/material';

export const styles = (): { [key: string]: SxProps } => ({
  containerMap: { marginTop: '15px' },
});

export const mapStyle = {
  width: '100%',
  height: '300px'
};
